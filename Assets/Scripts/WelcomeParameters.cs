using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WelcomeParameters : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameData.gameLife=5;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
